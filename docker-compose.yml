services:
  jupyter:
    build: .
    container_name: ml-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./workspace:/workspace

    # ── GPU support (NVIDIA) ───────────────────────────────────────────────────
    # Requires: nvidia-container-toolkit installed on the host.
    # Uncomment the block below to enable GPU passthrough.
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
